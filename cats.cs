using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp2.animal
{
    class cats:Animal
    {
        public cats(string name, int age, string vid, string capability) : base(name, age, vid, capability)
        {
        }
    }
}
